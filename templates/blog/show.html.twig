{% extends 'base.html.twig' %}

{% block title %}Blog{% endblock %}

{% block body %}
    <h1>Blog</h1>


    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ blog.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ blog.titre }}</td>
            </tr>
            <tr>
                <th> brochure </th>
                <td> <a href="{{ asset('uploads/brochures/' ~ blog.brochureFilename) }}">View brochure (PDF)</a>
                </td> 
            <tr>
                <th>Description</th>
                <td>{{ blog.content }}</td>
            </tr>
            
             

                    
        </tbody>
        

    </table>

    <a href="{{ path('blog_index') }}">back to list</a>

    <a href="{{ path('blog_edit', {'id': blog.id}) }}">edit</a>

    {{ include('blog/_delete_form.html.twig') }}
{% endblock %}

